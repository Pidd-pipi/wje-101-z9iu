<!-- 改编自：美食饮品主题项目提示词/wje-101/wje-101.md；后端由 Python + FastAPI + SQLAlchemy 改造为 Go 1.22 + Gin + GORM -->
# wje-101 咖啡品鉴社区

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**咖啡品鉴社区（wjecoffeetaste）** 是一个面向咖啡爱好者的全栈 Web 应用，用户可以记录每次咖啡品鉴的详细笔记（风味、评分、冲煮方式等），浏览和收藏豆种信息，创建和分享冲煮配方，并通过评论和点赞与其他用户互动，平台还提供个人主页统计功能，展示品鉴历史和偏好画像。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, username, email, password_hash, avatar, bio, role（user/admin）, created_at
  - 关键关系：拥有多个 TastingNote、BrewRecipe、Comment；可关注多个 User（自关联多对多 user_followers）
  - 业务动作：注册/登录、修改资料、上传头像、关注/取关
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/useUserStore.ts → frontend/src/pages/Profile.vue**

- **TastingNote（品鉴笔记）**
  - 字段：id, user_id, coffee_name, origin, roast_level（light/medium/dark）, flavor_tags（JSON）, aroma_score, acidity_score, body_score, overall_score, brew_method, brew_recipe_id（nullable）, notes_text, image_url, created_at, updated_at
  - 关键关系：属于 User，关联 BrewRecipe（可选），拥有多个 Comment 和 Like
  - 业务动作：创建/查看/编辑/删除笔记、点赞、评分、关联配方
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **BrewRecipe（冲煮配方）**
  - 字段：id, user_id, name, device, water_temp, grind_size, ratio, steps（JSON，每步含 step_number/description/duration_seconds）, created_at
  - 关键关系：属于 User，被多个 TastingNote 引用
  - 业务动作：创建/分享配方、按器具/水温筛选、关联到品鉴笔记
  - **此实体必须贯穿全栈**

- **CoffeeBean（豆种）**
  - 字段：id, name, origin, process_method（washed/natural/honey/anaerobic）, flavor_tags（JSON）, description, created_at
  - 关键关系：被多个 TastingNote 逻辑引用（通过咖啡名称）
  - 业务动作：管理员维护、用户浏览筛选、创建笔记时选择自动填充
  - **此实体必须贯穿全栈**

- **Comment（评论）**
  - 字段：id, note_id, user_id, content, created_at
  - 关键关系：属于 TastingNote 和 User
  - 业务动作：发表评论、删除自己的评论
  - **此实体必须贯穿全栈**

- **Like（点赞）**
  - 字段：id, user_id, note_id, created_at；唯一约束 (user_id, note_id)
  - 关键关系：连接 User 与 TastingNote
  - 业务动作：点赞/取消点赞、点赞计数
  - **此实体必须贯穿全栈**

- **UserFollow（关注）**
  - 字段：follower_id, following_id, created_at；复合主键 (follower_id, following_id)
  - 关键关系：User 自关联多对多
  - 业务动作：关注/取关、关注列表、粉丝统计
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/（首页）**
  - 消费实体：TastingNote、User、Like
  - 关键交互：最新品鉴笔记信息流、热门排行（按点赞数）、按烘焙度/产地筛选
  - 共享组件：`<ScoreStars>`、`<FlavorTags>`、`<UserAvatar>`

- **/note/create（创建品鉴笔记）**
  - 消费实体：User、CoffeeBean、BrewRecipe
  - 关键交互：选择豆种自动填充、选择烘焙度、添加风味标签、评分、关联配方、上传配图
  - 共享组件：`<ScoreStars>`、`<FlavorTags>`（可编辑模式）

- **/note/:id（品鉴详情）**
  - 消费实体：TastingNote、Comment、User、Like、BrewRecipe
  - 关键交互：评分展示、风味标签、关联配方卡片、点赞、评论、编辑/删除（仅作者）
  - 共享组件：`<ScoreStars>`、`<FlavorTags>`（只读）、`<UserAvatar>`、`<EmptyState>`

- **/beans（豆种库）**
  - 消费实体：CoffeeBean
  - 关键交互：按产地/处理法/风味标签筛选、关键词搜索、管理员新增/编辑/删除豆种
  - 共享组件：`<FlavorTags>`、`<EmptyState>`

- **/profile/:id（个人主页）**
  - 消费实体：User、TastingNote、UserFollow
  - 关键交互：关注/取关、品鉴次数/平均分/最爱产地 TOP3 统计、品鉴历史列表
  - 共享组件：`<UserAvatar>`、`<ScoreStars>`、`<EmptyState>`

- **/recipes（配方广场）**
  - 消费实体：BrewRecipe、User
  - 关键交互：按器具/水温筛选、搜索配方、查看配方步骤详情
  - 共享组件：`<EmptyState>`、`<UserAvatar>`

- **/login（登录/注册）**
  - 消费实体：User
  - 关键交互：登录、注册、切换登录/注册表单
  - 共享组件：无业务组件（使用 Element Plus 表单）

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 password_hash、role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中用户私有接口需 `AuthRequired()`，管理员维护豆种需 `RequireRole(admin)`
    - 前端：`router/index.ts` 路由守卫、`hooks/useAuth.ts`、`stores/useUserStore.ts`、`utils/request.ts` 请求拦截器

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 响应拦截器、`components/common/ErrorToast.vue`

- **请求日志**
  - 触达文件层：
    - 后端：`internal/middleware/logger.go`
    - 请求日志统一包含 request_id、method、path、status、latency_ms

### 5. 共享枚举/常量（≥ 2 组）

- **RoastLevel（烘焙度）**
  - 值：LIGHT = 'light'（浅烘）, MEDIUM = 'medium'（中烘）, DARK = 'dark'（深烘）
  - 前端定义位置：`frontend/src/constants/note.ts`
  - 后端定义位置：`backend/internal/constants/note.go`
  - 出现位置：TastingNote 模型、NoteService、ScoreStars 旁筛选器、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **ProcessMethod（处理法）**
  - 值：WASHED = 'washed'（水洗）, NATURAL = 'natural'（日晒）, HONEY = 'honey'（蜜处理）, ANAEROBIC = 'anaerobic'（厌氧发酵）
  - 前端定义位置：`frontend/src/constants/bean.ts`
  - 后端定义位置：`backend/internal/constants/bean.go`
  - 出现位置：CoffeeBean 模型、BeanService、豆种库筛选器、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **UserRole（用户角色）**
  - 值：USER = 'user', ADMIN = 'admin'
  - 前端定义位置：`frontend/src/constants/user.ts`
  - 后端定义位置：`backend/internal/constants/user.go`
  - 出现位置：User 模型、RBAC 中间件、路由权限、前端按钮显隐、路由守卫
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<ScoreStars>`：被 /、/note/create、/note/:id、/profile/:id 共用
- `<FlavorTags>`：被 /、/note/create、/note/:id、/beans 共用
- `<UserAvatar>`：被 /、/note/:id、/profile/:id、/recipes 共用
- `<EmptyState>`：被 /note/:id、/beans、/profile/:id、/recipes 共用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useAuth()`：登录/注册/登出、token 刷新、登录态与角色判断
- `usePagination()`：通用分页状态与翻页逻辑
- `utils/dateFormat.ts`：日期时间格式化
- `utils/storage.ts`：localStorage 封装（token 读写）

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `error_handler.go`：统一错误响应格式
- `logger.go`：请求日志记录
- `cors.go`：跨域配置

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`wjecoffeetaste`
- **前端端口**：28601
- **后端端口**：29601
- **前端框架**：Vue 3 + TypeScript
- **UI 库**：Element Plus + ECharts
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, note.ts, bean.ts, recipe.ts, comment.ts
├── stores/           # useUserStore.ts, useNoteStore.ts, useBeanStore.ts
├── types/            # user.ts, note.ts, bean.ts, recipe.ts, comment.ts, api.ts
├── components/common/# ScoreStars.vue, FlavorTags.vue, EmptyState.vue, UserAvatar.vue, ErrorToast.vue
├── hooks/            # useAuth.ts, usePagination.ts
├── pages/            # Home.vue, NoteCreate.vue, NoteDetail.vue, BeanLibrary.vue, Profile.vue, RecipeSquare.vue, Login.vue
├── router/           # index.ts（含路由守卫）
├── utils/            # request.ts, storage.ts, dateFormat.ts
└── constants/        # note.ts, bean.ts, user.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、JWT、端口等）
    ├── model/                   # 按实体分文件：user.go, user_follow.go, tasting_note.go, brew_recipe.go, coffee_bean.go, comment.go, like.go
    ├── repository/              # 按实体分文件：user_repository.go, tasting_note_repository.go, brew_recipe_repository.go, coffee_bean_repository.go, comment_repository.go, like_repository.go, user_follow_repository.go
    ├── service/                 # 按实体分文件：user_service.go, tasting_note_service.go, brew_recipe_service.go, coffee_bean_service.go, comment_service.go, like_service.go, user_follow_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, tasting_note_handler.go, brew_recipe_handler.go, coffee_bean_handler.go, comment_handler.go, like_handler.go, user_follow_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, notes.go, recipes.go, beans.go, comments.go
    ├── middleware/              # auth.go, rbac.go, error_handler.go, logger.go, cors.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # note.go, bean.go, user.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, pagination.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `NOTE_CREATE_SUCCESS`、`NOTE_LIKE_FAILED`
   - 任何业务字段变更（如 TastingNote 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"TastingNote[id=123] like failed: already liked"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、评分格式化、状态文本、角色文本、烘焙度文本、处理法文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **品鉴笔记评分/状态规则必须跨多处定义**
   - RoastLevel 与评分范围校验规则在 service、前端表单校验、日志模板、错误码、formatters 中同时存在
   - 新增一个规则需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - RoastLevel、ProcessMethod 和 UserRole 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 TastingNote 新增一个 'sweetness_score' 甜感评分字段"，必须触达：GORM AutoMigrate、TastingNote 模型、前后端 constants、NoteService、ScoreStars、Profile 统计图、formatters、日志模板、错误码、创建表单、详情页等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=wjecoffeetaste`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: wjecoffeetaste`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-wjecoffeetaste}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `28601:80`，后端 `${BACKEND_PORT:-29601}:8080`，数据库 `${DB_PORT:-57601}:5432`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=wjecoffeetaste`
- 数据库连接相关：`DB_NAME=coffeetaste`、`DB_USER=coffeetaste`、`DB_PASSWORD=coffeetaste_pwd`、`DB_ROOT_PASSWORD=coffeetaste_root`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=28601`、`BACKEND_PORT=29601`、`DB_PORT=57601`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 RoastLevel、ProcessMethod 和 UserRole 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:28601` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<ScoreStars>` 或 `<FlavorTags>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
